name: Use Custom Action

on:
  push:
    branches:
      - main

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Use our custom action
        uses: ./
        with:
          name: "Go Developer" 