name: Example

on:
  push:
    branches:
      - test/action

jobs:
  example:
    runs-on: ubuntu-latest
    steps:
      - uses: leedonggyu/terradrift@v1.0.2
        with:   
          name: "leedonggyu" 